---
# =============================================================================
# LIST TEMPLATE
# =============================================================================
# Copy this file to: src/content/fandoms/<fandom>/lists/<list-slug>.mdx
#
# A list defines an ordered way to experience entries (chronological, release
# order, custom order, etc.). Lists reference entries by ID.
#
# Lists support 1-4 layer hierarchy for visual grouping (eras, arcs, etc.)
# Progress is tracked per-list, not per-entry.
# =============================================================================

# REQUIRED FIELDS
# -----------------------------------------------------------------------------

# URL-safe identifier (should match filename without extension)
id: 'list-slug'

# Display title
title: 'Chronological Order'

# Brief description
description: 'Experience the saga in timeline order.'

# OPTIONAL FIELDS
# -----------------------------------------------------------------------------

# Cover image for list cards
coverImage: '@/assets/fandoms/fandom-slug/lists/list-slug.jpg'

# Estimated total runtime (computed if not provided)
# Format: "42h 30m" or minutes as number
estimatedRuntime: '42h 30m'

# Tags for filtering
tags:
  - 'recommended'
  - 'complete'

# STRUCTURE
# =============================================================================
# The 'structure' field defines the ordered hierarchy of entries.
#
# Structure uses 2 levels:
#   SAGA (Level 1): Major grouping with boxed design (e.g., "The Prequel Era")
#   ARC (Level 2): Sub-grouping with simple heading (e.g., "The Clone Wars")
#
# SAGA level can have:
#   - title: Display name (shown in boxed design)
#   - description (optional): Context or summary
#   - entries (optional): Array of entry IDs directly in saga
#   - children (optional): Array of arcs within this saga
#
# ARC level can have:
#   - title: Display name (shown as simple heading)
#   - description (optional): Brief context
#   - entries (optional): Array of entry IDs
#   - NO children (arcs cannot be nested)
#
# Entries are referenced by their ID (filename without extension).
# The order in this file IS the canonical order.
# =============================================================================

structure:
  # SAGA with ARCs
  - title: 'The Prequel Era'
    description: 'The fall of the Republic and rise of the Empire (32 BBY - 19 BBY)'
    children:
      - title: 'Episode I'
        entries:
          - the-phantom-menace
      - title: 'Episode II & The Clone Wars'
        description: 'The galaxy-spanning conflict'
        entries:
          - attack-of-the-clones
          - clone-wars-movie
          - clone-wars-s01e01
          - clone-wars-s01e02
      - title: 'Episode III'
        entries:
          - revenge-of-the-sith

  # SAGA with direct entries (no arcs)
  - title: 'The Original Trilogy'
    description: 'The Galactic Civil War (0 BBY - 4 ABY)'
    entries:
      - a-new-hope
      - empire-strikes-back
      - return-of-the-jedi

# =============================================================================
# ALTERNATIVE: Flat structure (no hierarchy)
# =============================================================================
# If you don't need visual grouping, use a simple flat list:
#
# structure:
#   - entries:
#       - entry-one
#       - entry-two
#       - entry-three
# =============================================================================
---

{/_
Optional MDX content below frontmatter.
Use for viewing guides, recommendations, or context.
_/}

## How to Use This List

Explain the reasoning behind this order, any skippable entries, or viewing recommendations.

## Notes

- **First time?** Consider starting with [another list] for a simpler introduction.
- **Skippable:** Some entries are optional and marked accordingly.
